<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/security
http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <global-method-security secured-annotations="enabled" />

    <http auto-config="true" use-expressions="true">
    <!--<http access-denied-page="/error403.jsp">-->
        <intercept-url pattern="/documents*" access="hasRole('ROLE_ADMIN')"/>
        <!--<intercept-url pattern="/users/**" access="hasRole('ROLE_ADMIN')"/>-->
        <!--<intercept-url pattern="/desktop/**" access="isAuthenticated()"/>-->

        <form-login login-processing-url="/static/j_spring_security_check" login-page="/signin"
                    authentication-failure-url="/signin?error=true" default-target-url="/desktop"/>
        <logout logout-url="/static/j_spring_security_logout" logout-success-url="/desktop"/>
        <http-basic />
        <anonymous username="guest" granted-authority="ROLE_ANONYMOUS"/>
        <remember-me token-validity-seconds="2419200" />
    </http>

    <!--<user-service id="userService">-->
        <!--<user name="user" password="user" authorities="ROLE_USER"/>-->
        <!--<user name="admin" password="admin" authorities="ROLE_ADMIN"/>-->
    <!--</user-service>-->

    <jdbc-user-service id="userService"
                       data-source-ref="dataSource"
                       users-by-username-query=
                               "select email as username, password, active from user where email=?"
                       authorities-by-username-query=
                               "select email as username,'ROLE_PUBLISHER' from user where email=?" />

    <authentication-manager>
        <authentication-provider user-service-ref="userService">
            <password-encoder hash="plaintext" />
        </authentication-provider>
    </authentication-manager>

</beans:beans>